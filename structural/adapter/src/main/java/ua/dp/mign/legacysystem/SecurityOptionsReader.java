package ua.dp.mign.legacysystem;

/*
 * This is representation of a legacy interface
 * that can't be used as is in the new system.
 */
public interface SecurityOptionsReader {
    String hasAccess(String option);
}
